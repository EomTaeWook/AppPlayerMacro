<Controls:MetroWindow x:Class="Macro.MainWindow"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:Macro.View"
        xmlns:util="clr-namespace:Utils;assembly=Utils"
        mc:Ignorable="d"
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        Title="매크로" Height="780" Width="1270">
    <Window.Resources>
        
    </Window.Resources>
    <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" VerticalAlignment="Stretch">
        <Border BorderBrush="Black" BorderThickness="2" DockPanel.Dock="Top" Height="110">
            <DockPanel LastChildFill="True" Margin="0,0,0,6">
                <Label Height="43" VerticalAlignment="Top" Content="{Binding Path=MainWindowViewModel.SelectProcess, Source={StaticResource Locator}}" DockPanel.Dock="Top"/>
                <Grid>
                    <Grid.RowDefinitions>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="9*"></ColumnDefinition>
                        <ColumnDefinition Width="0.3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" Name="combo_process" Height="40" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" DockPanel.Dock="Top">
                    </ComboBox>
                    <Button Style="{DynamicResource MetroCircleButtonStyle}" x:Name="btnRefresh" Content="&#81;" Grid.Column="1" Width="40" Height="40" FontFamily="Wingdings 3">
                    </Button>
                </Grid>
            </DockPanel>
        </Border>
        <Grid Width="auto">
            <Grid.RowDefinitions>
                <RowDefinition>
                </RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border BorderBrush="Black" BorderThickness="1">
                <DockPanel LastChildFill="True" Grid.Column="0" Grid.Row="0">
                    <StackPanel DockPanel.Dock="Top">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="2*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Row="0" Grid.Column="0" Content="{Binding Path=MainWindowViewModel.CompareImage, Source={StaticResource Locator}}" ></Label>
                            <DockPanel Grid.Row="0" Grid.Column="1">
                                <Button Name="btnCapture" Content="{Binding Path=MainWindowViewModel.ScreenCapture, Source={StaticResource Locator}}"/>
                            </DockPanel>
                        </Grid>
                    </StackPanel>
                    <Canvas Name="captureImage" Background="White" Grid.Row="0">
                    </Canvas>
                </DockPanel>
            </Border>
            <DockPanel LastChildFill="True" Grid.Column="1" Grid.Row="0">
                <StackPanel DockPanel.Dock="Top">
                    <DockPanel LastChildFill="False">
                        <Label DockPanel.Dock="Left" FontSize="30" Content="{Binding Path=MainWindowViewModel.Config, Source={StaticResource Locator}}"></Label>
                        <Button Name="btnDelete" DockPanel.Dock="Right" Visibility="Collapsed" Margin="5, 5, 5, 5" Content="{Binding Path=MainWindowViewModel.Delete, Source={StaticResource Locator}}"></Button>
                        <Button Name="btnSave" DockPanel.Dock="Right" Margin="5, 5, 5, 5" Content="{Binding Path=MainWindowViewModel.Save, Source={StaticResource Locator}}"></Button>
                    </DockPanel>
                </StackPanel>
                <StackPanel DockPanel.Dock="Top">
                    <view:ConfigEventView x:Name="configControl"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </DockPanel>
</Controls:MetroWindow>
