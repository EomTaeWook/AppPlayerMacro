<UserControl x:Class="Macro.View.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d">
    <Grid>
        <DockPanel LastChildFill="False">
            <Grid DockPanel.Dock="Top">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                    <ColumnDefinition Width="6*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" FontSize="15" Content="{Binding Path=LabelViewModel.Language, Source={StaticResource Locator}}"></Label>
                <ComboBox Name="comboLanguage" Grid.Row="0" Grid.Column="1" FontSize="15"></ComboBox>
                <Label Grid.Row="1" Grid.Column="0" FontSize="15" Content="{Binding Path=LabelViewModel.SavePath, Source={StaticResource Locator}}"></Label>
                <TextBox Name="txtSavePath" Grid.Row="1" Grid.Column="1" Style="{DynamicResource SearchMetroTextBox}" Controls:TextBoxHelper.ButtonCommand="{Binding TextBoxButtonCmd, Mode=OneWay}"/>
                <Label Grid.Row="2" Grid.Column="0"  FontSize="15" Content="{Binding Path=LabelViewModel.Period, Source={StaticResource Locator}}"></Label>
                <Controls:NumericUpDown Name="numPeriod" Minimum="2000" Grid.Row="2" Grid.Column="1"></Controls:NumericUpDown>
                <Label Grid.Row="3" Grid.Column="0"  FontSize="15" Content="{Binding Path=LabelViewModel.ProcessDelay, Source={StaticResource Locator}}"></Label>
                <Controls:NumericUpDown Name="numDelay" Minimum="0" Grid.Row="3" Grid.Column="1"></Controls:NumericUpDown>
                <Label Grid.Row="4" Grid.Column="0" FontSize="15" Content="{Binding Path=LabelViewModel.Similarity, Source={StaticResource Locator}}"></Label>
                <Controls:NumericUpDown Name="numSimilarity" Minimum="20" Grid.Row="4" Grid.Column="1"></Controls:NumericUpDown>
            </Grid>
            <Button Name="btnSave" DockPanel.Dock="Bottom" Content="{Binding Path=LabelViewModel.Save, Source={StaticResource Locator}}" Style="{DynamicResource SquareButtonStyle}"></Button>
        </DockPanel>
    </Grid>
</UserControl>
